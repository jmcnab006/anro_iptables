---
- name: Prepare
  hosts: all
  become: true

  tasks:
    - name: PREPARE | Install test dependencies
      ansible.builtin.apt:
        name:
          - iputils-ping
        state: present
        update_cache: true